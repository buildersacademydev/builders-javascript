/**
 * Challenge 02: Number Converter
 * 
 * Base conversion system using loops and math.
 */

/**
 * TODO: Convert decimal to binary string
 * @param {number} num - Decimal number
 * @returns {string} - Binary representation
 */
export function decimalToBinary(num) {
  // TODO: Convert to binary string
}

/**
 * TODO: Convert binary string to decimal
 * @param {string} binary - Binary string
 * @returns {number} - Decimal number
 */
export function binaryToDecimal(binary) {
  // TODO: Convert binary to decimal
}

/**
 * TODO: Convert decimal to hexadecimal
 * @param {number} num - Decimal number
 * @returns {string} - Hexadecimal string
 */
export function decimalToHex(num) {
  // TODO: Convert to hexadecimal
}

/**
 * TODO: Convert hexadecimal to decimal
 * @param {string} hex - Hexadecimal string
 * @returns {number} - Decimal number
 */
export function hexToDecimal(hex) {
  // TODO: Convert hex to decimal
}

/**
 * TODO: Convert decimal to any base (2-36)
 * @param {number} num - Decimal number
 * @param {number} base - Target base (2-36)
 * @returns {string} - Number in target base
 */
export function decimalToBase(num, base) {
  // TODO: Convert to any base
}

/**
 * TODO: Convert from any base to decimal
 * @param {string} num - Number in source base
 * @param {number} base - Source base (2-36)
 * @returns {number} - Decimal number
 */
export function baseToDecimal(num, base) {
  // TODO: Convert from any base
}
