/**
 * Challenge 03: Text Analyzer
 * 
 * String statistics tool using loops and string methods.
 */

/**
 * TODO: Count words in text
 * Words are separated by whitespace
 * @param {string} text - Text to analyze
 * @returns {number} - Word count
 */
export function countWords(text) {
  // TODO: Count words in text
}

/**
 * TODO: Count character frequency
 * @param {string} text - Text to analyze
 * @returns {object} - Map of character to count
 */
export function countCharFrequency(text) {
  // TODO: Count each character occurrence
}

/**
 * TODO: Find most frequent character(s)
 * @param {string} text - Text to analyze
 * @returns {object} - { chars: Array, count: number }
 */
export function mostFrequentChar(text) {
  // TODO: Find most frequent character(s)
}

/**
 * TODO: Calculate average word length
 * @param {string} text - Text to analyze
 * @returns {number} - Average word length
 */
export function averageWordLength(text) {
  // TODO: Calculate average word length
}

/**
 * TODO: Find longest word(s)
 * @param {string} text - Text to analyze
 * @returns {object} - { words: Array, length: number }
 */
export function longestWords(text) {
  // TODO: Find longest word(s)
}

/**
 * TODO: Check if text is palindrome (ignoring spaces and case)
 * @param {string} text - Text to check
 * @returns {boolean} - True if palindrome
 */
export function isPalindrome(text) {
  // TODO: Check if palindrome
}
