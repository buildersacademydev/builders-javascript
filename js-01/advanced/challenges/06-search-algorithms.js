/**
 * Challenge 06: Search Algorithms
 * 
 * Linear and binary search implementations.
 */

/**
 * TODO: Linear search - find first occurrence of target
 * @param {Array} arr - Array to search
 * @param {any} target - Value to find
 * @returns {number} - Index of target, or -1 if not found
 */
export function linearSearch(arr, target) {
  // TODO: Implement linear search
}

/**
 * TODO: Binary search (array must be sorted)
 * @param {Array} arr - Sorted array to search
 * @param {any} target - Value to find
 * @returns {number} - Index of target, or -1 if not found
 */
export function binarySearch(arr, target) {
  // TODO: Implement binary search
}

/**
 * TODO: Find all occurrences of target
 * @param {Array} arr - Array to search
 * @param {any} target - Value to find
 * @returns {Array} - Array of all indices where target appears
 */
export function findAllOccurrences(arr, target) {
  // TODO: Find all occurrences
}

/**
 * TODO: Find minimum and maximum values in array
 * @param {Array} arr - Array of numbers
 * @returns {object} - { min: value, max: value, minIndex: index, maxIndex: index }
 */
export function findMinMax(arr) {
  // TODO: Find min and max with their indices
}

/**
 * TODO: Search for substring in string using simple algorithm
 * @param {string} text - Text to search in
 * @param {string} pattern - Pattern to find
 * @returns {number} - Index of first match, or -1 if not found
 */
export function substringSearch(text, pattern) {
  // TODO: Find pattern in text
}

/**
 * TODO: Find first element matching a condition
 * @param {Array} arr - Array to search
 * @param {Function} condition - Function returning boolean
 * @returns {any} - First matching element, or undefined
 */
export function findFirstMatch(arr, condition) {
  // TODO: Find first element matching condition
}
