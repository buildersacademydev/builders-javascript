/**
 * Challenge 01: Console Basics
 * 
 * Learn to use console.log for output and debugging.
 */

/**
 * TODO: Create a greeting message
 * @param {string} name - The name to greet
 * @returns {string} - A greeting message in format "Hello, {name}!"
 * 
 * Example: createGreeting("John") should return "Hello, John!"
 */
export function createGreeting(name) {
  // TODO: Implement this function
}

/**
 * TODO: Log a message to console and return it
 * @param {string} message - The message to log
 * @returns {string} - The same message that was logged
 */
export function logAndReturn(message) {
  // TODO: Implement this function
}

/**
 * TODO: Create a formatted message with multiple values
 * @param {string} name - Person's name
 * @param {number} age - Person's age
 * @param {string} city - Person's city
 * @returns {string} - Format: "{name} is {age} years old and lives in {city}"
 * 
 * Example: formatPersonInfo("Alice", 25, "Mumbai") 
 *          returns "Alice is 25 years old and lives in Mumbai"
 */
export function formatPersonInfo(name, age, city) {
  // TODO: Implement this function
}

/**
 * TODO: Create a debug message showing variable info
 * @param {string} varName - Variable name
 * @param {any} value - Variable value
 * @returns {string} - Format: "DEBUG: {varName} = {value}"
 * 
 * Example: debugVariable("score", 95) returns "DEBUG: score = 95"
 */
export function debugVariable(varName, value) {
  // TODO: Implement this function
}

/**
 * TODO: Create a separator line for console output
 * @param {number} length - Length of separator
 * @param {string} char - Character to use (default: "-")
 * @returns {string} - A string of repeated characters
 * 
 * Example: createSeparator(5, "*") returns "*****"
 */
export function createSeparator(length, char = "-") {
  // TODO: Implement this function
}
