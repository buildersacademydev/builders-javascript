/**
 * Challenge 03: Data Types
 * 
 * Work with primitive data types: String, Number, Boolean, Undefined, Null
 */

/**
 * TODO: Return the type of a value
 * @param {any} value - Any value
 * @returns {string} - The type of the value
 */
export function getValueType(value) {
  // TODO: Use typeof operator
}

/**
 * TODO: Create a string from a number
 * @param {number} num - A number
 * @returns {string} - The number as a string (not using String())
 */
export function numberToString(num) {
  // TODO: Convert number to string without using String()
}

/**
 * TODO: Check if a value is a string
 * @param {any} value - Any value
 * @returns {boolean} - True if string, false otherwise
 */
export function isString(value) {
  // TODO: Check if value is string type
}

/**
 * TODO: Check if a value is a number
 * @param {any} value - Any value
 * @returns {boolean} - True if number type, false otherwise
 * Note: NaN should return false
 */
export function isNumber(value) {
  // TODO: Check if value is number type and not NaN
}

/**
 * TODO: Check if a value is boolean
 * @param {any} value - Any value
 * @returns {boolean} - True if boolean, false otherwise
 */
export function isBoolean(value) {
  // TODO: Check if value is boolean type
}

/**
 * TODO: Check if a value is undefined
 * @param {any} value - Any value
 * @returns {boolean} - True if undefined, false otherwise
 */
export function isUndefined(value) {
  // TODO: Check if value is undefined
}

/**
 * TODO: Check if a value is null
 * @param {any} value - Any value
 * @returns {boolean} - True if null, false otherwise
 */
export function isNull(value) {
  // TODO: Check if value is null (typeof won't work here!)
}

/**
 * TODO: Get detailed type info including null distinction
 * @param {any} value - Any value
 * @returns {string} - "null" for null, otherwise typeof result
 */
export function getDetailedType(value) {
  // TODO: Return "null" for null values, otherwise typeof
}
