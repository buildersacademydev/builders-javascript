/**
 * Challenge 05: Equality
 * 
 * Practice == (loose equality) vs === (strict equality)
 */

/**
 * TODO: Check loose equality (==)
 * @param {any} a - First value
 * @param {any} b - Second value
 * @returns {boolean} - Result of a == b
 */
export function looseEqual(a, b) {
  // TODO: Return a == b
}

/**
 * TODO: Check strict equality (===)
 * @param {any} a - First value
 * @param {any} b - Second value
 * @returns {boolean} - Result of a === b
 */
export function strictEqual(a, b) {
  // TODO: Return a === b
}

/**
 * TODO: Check if two values are not loosely equal
 * @param {any} a - First value
 * @param {any} b - Second value
 * @returns {boolean} - Result of a != b
 */
export function looseNotEqual(a, b) {
  // TODO: Return a != b
}

/**
 * TODO: Check if two values are not strictly equal
 * @param {any} a - First value
 * @param {any} b - Second value
 * @returns {boolean} - Result of a !== b
 */
export function strictNotEqual(a, b) {
  // TODO: Return a !== b
}

/**
 * TODO: Determine if two values are truthy or falsy in the same way
 * @param {any} a - First value
 * @param {any} b - Second value
 * @returns {boolean} - True if both are truthy or both are falsy
 */
export function sameTruthyValue(a, b) {
  // TODO: Check if both have same truthiness (both truthy or both falsy)
}

/**
 * TODO: Check if a value is truthy
 * @param {any} value - Any value
 * @returns {boolean} - True if truthy, false if falsy
 */
export function isTruthy(value) {
  // TODO: Check if value is truthy
}

/**
 * TODO: Check if a value is falsy
 * @param {any} value - Any value
 * @returns {boolean} - True if falsy, false if truthy
 */
export function isFalsy(value) {
  // TODO: Check if value is falsy
}

/**
 * TODO: Compare with null in both loose and strict equality
 * Returns object with both results
 * @param {any} value - Value to compare with null
 * @returns {object} - { loose: boolean, strict: boolean }
 */
export function compareWithNull(value) {
  // TODO: Return object with loose and strict equality results vs null
}
