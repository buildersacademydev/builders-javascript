/**
 * Challenge 08: Loops
 * 
 * Practice for, while, and do-while loops.
 */

/**
 * TODO: Calculate sum of numbers from 1 to n using for loop
 * @param {number} n - Upper limit
 * @returns {number} - Sum of 1 to n
 */
export function sumToN(n) {
  // TODO: Use for loop to calculate sum
}

/**
 * TODO: Calculate factorial of n using while loop
 * Factorial: n! = n * (n-1) * (n-2) * ... * 1
 * @param {number} n - Number to calculate factorial for
 * @returns {number} - Factorial of n (0! = 1)
 */
export function factorial(n) {
  // TODO: Use while loop to calculate factorial
}

/**
 * TODO: Count digits in a number using do-while loop
 * @param {number} num - Number to count digits
 * @returns {number} - Count of digits
 */
export function countDigits(num) {
  // TODO: Use do-while to count digits
}

/**
 * TODO: Generate multiplication table for a number
 * @param {number} num - Number for table
 * @param {number} limit - Up to which multiple (default: 10)
 * @returns {Array} - Array of strings like "5 x 1 = 5", "5 x 2 = 10", etc.
 */
export function multiplicationTable(num, limit = 10) {
  // TODO: Use loop to generate table entries
}

/**
 * TODO: Check if a number is prime
 * @param {number} num - Number to check
 * @returns {boolean} - True if prime, false otherwise
 */
export function isPrime(num) {
  // TODO: Check if number is prime using loop
}

/**
 * TODO: Generate Fibonacci sequence up to n terms
 * @param {number} n - Number of terms
 * @returns {Array} - Array of Fibonacci numbers
 */
export function fibonacci(n) {
  // TODO: Generate Fibonacci sequence
}

/**
 * TODO: Reverse a string using a loop
 * @param {string} str - String to reverse
 * @returns {string} - Reversed string
 */
export function reverseString(str) {
  // TODO: Use loop to reverse string
}

/**
 * TODO: Find the largest number in an array using a loop
 * @param {Array} arr - Array of numbers
 * @returns {number} - Largest number
 */
export function findLargest(arr) {
  // TODO: Use loop to find largest
}
