/**
 * Challenge 06: FizzBuzz
 * 
 * The classic programming challenge combining all concepts taught.
 */

/**
 * TODO: Classic FizzBuzz for a single number
 * Return "Fizz" if divisible by 3
 * Return "Buzz" if divisible by 5
 * Return "FizzBuzz" if divisible by both
 * Return the number as string otherwise
 * @param {number} num - Number to check
 * @returns {string} - FizzBuzz result
 */
export function fizzBuzz(num) {
  // TODO: Implement FizzBuzz logic
}

/**
 * TODO: Generate FizzBuzz sequence from 1 to n
 * @param {number} n - Upper limit
 * @returns {Array} - Array of FizzBuzz results
 */
export function fizzBuzzSequence(n) {
  // TODO: Generate complete sequence
}

/**
 * TODO: Count Fizz, Buzz, and FizzBuzz in a sequence
 * @param {number} n - Upper limit of sequence
 * @returns {object} - { fizz: count, buzz: count, fizzBuzz: count, number: count }
 */
export function countFizzBuzz(n) {
  // TODO: Count occurrences
}

/**
 * TODO: FizzBuzz with custom rules
 * @param {number} num - Number to check
 * @param {Array} rules - Array of { divisor: number, word: string }
 * @returns {string} - Concatenated words or number
 * @example
 * fizzBuzzCustom(15, [
 *   { divisor: 3, word: "Fizz" },
 *   { divisor: 5, word: "Buzz" }
 * ]) returns "FizzBuzz"
 */
export function fizzBuzzCustom(num, rules) {
  // TODO: Implement custom FizzBuzz
}

/**
 * TODO: Find the nth FizzBuzz number (divisible by 3 and 5)
 * @param {number} n - Which FizzBuzz number to find (1st, 2nd, etc.)
 * @returns {number} - The nth FizzBuzz number
 */
export function findNthFizzBuzz(n) {
  // TODO: Find nth number divisible by 15
}

/**
 * TODO: Check if a FizzBuzz sequence is correct
 * @param {Array} sequence - Array to validate
 * @param {number} start - Starting number
 * @returns {boolean} - True if sequence is correct
 */
export function isValidFizzBuzzSequence(sequence, start = 1) {
  // TODO: Validate the sequence
}
