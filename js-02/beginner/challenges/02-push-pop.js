/**
 * Challenge 02: Push and Pop
 * 
 * Learn to add and remove elements from the end of an array.
 */

/**
 * TODO: Add an element to the end of an array
 * @param {any[]} arr - Input array
 * @param {any} element - Element to add
 * @returns {number} - New length of the array
 */
export function pushElement(arr, element) {
  // TODO: Use push() to add element and return new length
}

/**
 * TODO: Remove and return the last element
 * @param {any[]} arr - Input array
 * @returns {any} - The removed element (undefined if array is empty)
 */
export function popElement(arr) {
  // TODO: Use pop() to remove and return last element
}

/**
 * TODO: Add multiple elements to end of array
 * @param {any[]} arr - Input array
 * @param {any[]} elements - Elements to add
 * @returns {number} - New length of the array
 */
export function pushMultiple(arr, elements) {
  // TODO: Add all elements to the array and return new length
}

/**
 * TODO: Remove and return last N elements
 * @param {any[]} arr - Input array
 * @param {number} n - Number of elements to remove
 * @returns {any[]} - Array of removed elements
 */
export function popMultiple(arr, n) {
  // TODO: Remove last n elements and return them as array
}

/**
 * TODO: Push element if it doesn't exist
 * @param {any[]} arr - Input array
 * @param {any} element - Element to add
 * @returns {boolean} - True if added, false if already exists
 */
export function pushUnique(arr, element) {
  // TODO: Only push if element is not already in array
}

/**
 * TODO: Pop all elements and return them
 * @param {any[]} arr - Input array
 * @returns {any[]} - All removed elements (empty array becomes empty array)
 */
export function popAll(arr) {
  // TODO: Remove all elements and return them
}

/**
 * TODO: Swap first and last elements
 * @param {any[]} arr - Input array (must have at least 2 elements)
 * @returns {any[]} - The modified array
 */
export function swapFirstLast(arr) {
  // TODO: Swap first and last elements
}

/**
 * TODO: Remove last element only if array is not empty
 * @param {any[]} arr - Input array
 * @returns {boolean} - True if element was removed, false if array was empty
 */
export function popIfNotEmpty(arr) {
  // TODO: Only pop if array has elements
}
