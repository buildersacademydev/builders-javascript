/**
 * Challenge 03: Map Method
 * 
 * Learn to transform arrays using the map() method.
 */

/**
 * TODO: Double each number in an array
 * @param {number[]} numbers - Array of numbers
 * @returns {number[]} - Array with each number doubled
 */
export function doubleNumbers(numbers) {
  // TODO: Use map() to double each number
}

/**
 * TODO: Convert strings to uppercase
 * @param {string[]} strings - Array of strings
 * @returns {string[]} - Array with strings uppercased
 */
export function toUpperCaseStrings(strings) {
  // TODO: Use map() to convert to uppercase
}

/**
 * TODO: Get full names from first and last names
 * @param {string[]} firstNames - Array of first names
 * @param {string[]} lastNames - Array of last names
 * @returns {string[]} - Array of full names
 */
export function combineNames(firstNames, lastNames) {
  // TODO: Use map() to combine first and last names
}

/**
 * TODO: Add a prefix to each string
 * @param {string[]} items - Array of strings
 * @param {string} prefix - Prefix to add
 * @returns {string[]} - Array with prefix added
 */
export function addPrefix(items, prefix) {
  // TODO: Use map() to add prefix to each item
}

/**
 * TODO: Get object with name and greeting
 * @param {string[]} names - Array of names
 * @returns {object[]} - Array of objects with name and greeting
 */
export function createGreetings(names) {
  // TODO: Use map() to create greeting objects
}

/**
 * TODO: Extract specific property from objects
 * @param {object[]} objects - Array of objects
 * @param {string} property - Property to extract
 * @returns {any[]} - Array of property values
 */
export function extractProperty(objects, property) {
  // TODO: Use map() to extract the property
}

/**
 * TODO: Square numbers and add 1
 * @param {number[]} numbers - Array of numbers
 * @returns {number[]} - Array with (n * n + 1) for each number
 */
export function squareAndAddOne(numbers) {
  // TODO: Use map() to compute square + 1
}

/**
 * TODO: Convert array of numbers to array of strings
 * @param {number[]} numbers - Array of numbers
 * @returns {string[]} - Array of string representations
 */
export function numbersToStrings(numbers) {
  // TODO: Use map() to convert numbers to strings
}
