/**
 * Challenge 04: Filter Method
 * 
 * Learn to filter arrays using the filter() method.
 */

/**
 * TODO: Filter even numbers
 * @param {number[]} numbers - Array of numbers
 * @returns {number[]} - Array with only even numbers
 */
export function filterEvens(numbers) {
  // TODO: Use filter() to get only even numbers
}

/**
 * TODO: Filter numbers greater than a threshold
 * @param {number[]} numbers - Array of numbers
 * @param {number} threshold - Minimum value to include
 * @returns {number[]} - Numbers greater than threshold
 */
export function filterGreaterThan(numbers, threshold) {
  // TODO: Use filter() to filter numbers
}

/**
 * TODO: Filter strings by length
 * @param {string[]} strings - Array of strings
 * @param {number} minLength - Minimum string length
 * @returns {string[]} - Strings with length >= minLength
 */
export function filterByLength(strings, minLength) {
  // TODO: Use filter() to filter by length
}

/**
 * TODO: Filter objects by property value
 * @param {object[]} objects - Array of objects
 * @param {string} property - Property to check
 * @param {any} value - Value to match
 * @returns {object[]} - Objects where property equals value
 */
export function filterByProperty(objects, property, value) {
  // TODO: Use filter() to filter by property
}

/**
 * TODO: Remove falsy values from array
 * @param {any[]} arr - Array with potential falsy values
 * @returns {any[]} - Array with only truthy values
 */
export function removeFalsy(arr) {
  // TODO: Use filter() to remove falsy values
}

/**
 * TODO: Filter unique values (remove duplicates)
 * @param {any[]} arr - Array with potential duplicates
 * @returns {any[]} - Array with only unique values
 */
export function filterUnique(arr) {
  // TODO: Use filter() to get unique values
}

/**
 * TODO: Filter strings that contain a substring
 * @param {string[]} strings - Array of strings
 * @param {string} substring - Substring to search for
 * @returns {string[]} - Strings containing the substring
 */
export function filterContains(strings, substring) {
  // TODO: Use filter() to find strings containing substring
}

/**
 * TODO: Filter numbers in a range
 * @param {number[]} numbers - Array of numbers
 * @param {number} min - Minimum value (inclusive)
 * @param {number} max - Maximum value (inclusive)
 * @returns {number[]} - Numbers within the range
 */
export function filterInRange(numbers, min, max) {
  // TODO: Use filter() to filter numbers in range
}
