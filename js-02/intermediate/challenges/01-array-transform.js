/**
 * Challenge 01: Array Transformation
 * 
 * Transform data using map and filter combinations.
 */

/**
 * TODO: Transform student data to include grade status
 * @param {object[]} students - Array of students with name and score
 * @returns {object[]} - Students with pass status added
 */
export function transformStudents(students) {
  // TODO: Map to add pass: true/false based on score >= 60
}

/**
 * TODO: Get email domains from emails
 * @param {string[]} emails - Array of email addresses
 * @returns {string[]} - Array of unique domains
 */
export function getEmailDomains(emails) {
  // TODO: Extract domain from email and return unique domains
}

/**
 * TODO: Normalize array of strings to lowercase and trim
 * @param {string[]} strings - Array of strings with potential whitespace
 * @returns {string[]} - Normalized lowercase strings
 */
export function normalizeStrings(strings) {
  // TODO: Map to trim and lowercase each string
}

/**
 * TODO: Extract and capitalize first letter of each word
 * @param {string[]} words - Array of words
 * @returns {string[]} - Array of capitalized words
 */
export function capitalizeWords(words) {
  // TODO: Map to capitalize first letter of each word
}

/**
 * TODO: Filter valid numbers and double them
 * @param {any[]} values - Array of mixed values
 * @returns {number[]} - Doubled valid numbers
 */
export function filterAndDoubleNumbers(values) {
  // TODO: Filter only numbers, then double each
}

/**
 * TODO: Create username from email
 * @param {string} email - Email address
 * @returns {string} - Username (part before @)
 */
export function emailToUsername(email) {
  // TODO: Extract username from email
}

/**
 * TODO: Convert temperatures
 * @param {number[]} celsius - Array of temperatures in Celsius
 * @returns {number[]} - Array of temperatures in Fahrenheit
 */
export function celsiusToFahrenheit(celsius) {
  // TODO: Convert using formula: F = C * 9/5 + 32
}

/**
 * TODO: Get full paths from relative paths
 * @param {string[]} paths - Array of relative paths
 * @param {string} basePath - Base path to prepend
 * @returns {string[]} - Full paths
 */
export function getFullPaths(paths, basePath) {
  // TODO: Map to prepend basePath to each path
}

/**
 * TODO: Filter objects with valid ages (0-150)
 * @param {object[]} people - Array of people with age
 * @returns {object[]} - People with valid ages
 */
export function filterValidAges(people) {
  // TODO: Filter people where age is between 0 and 150
}

/**
 * TODO: Parse string numbers to actual numbers
 * @param {string[]} strNumbers - Array of number strings
 * @returns {number[]} - Array of actual numbers
 */
export function parseNumbers(strNumbers) {
  // TODO: Map to convert strings to numbers
}

/**
 * TODO: Create object mapping id to name
 * @param {object[]} items - Array of items with id and name
 * @returns {object} - Object with id as key, name as value
 */
export function mapIdToName(items) {
  // TODO: Reduce to create mapping object
}

/**
 * TODO: Get names sorted alphabetically
 * @param {object[]} people - Array of people with name
 * @returns {string[]} - Sorted array of names
 */
export function getSortedNames(people) {
  // TODO: Map to extract names, then sort
}
