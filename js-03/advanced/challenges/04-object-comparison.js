/**
 * Challenge 04: Object Comparison
 * 
 * Compare objects in various ways.
 */

/**
 * TODO: Shallow compare two objects
 * @param {object} obj1 - First object
 * @param {object} obj2 - Second object
 * @returns {boolean} - True if equal
 */
export function shallowCompare(obj1, obj2) {
  // TODO: Compare objects shallowly
}

/**
 * TODO: Deep compare two objects
 * @param {object} obj1 - First object
 * @param {object} obj2 - Second object
 * @returns {boolean} - True if equal
 */
export function deepCompare(obj1, obj2) {
  // TODO: Compare objects deeply
}

/**
 * TODO: Compare by specific keys
 * @param {object} obj1 - First object
 * @param {object} obj2 - Second object
 * @param {string[]} keys - Keys to compare
 * @returns {boolean} - True if equal
 */
export function compareKeys(obj1, obj2, keys) {
  // TODO: Compare specific keys
}

/**
 * TODO: Get differences between objects
 * @param {object} obj1 - First object
 * @param {object} obj2 - Second object
 * @returns {object} - Object with differences
 */
export function getDifferences(obj1, obj2) {
  // TODO: Get all differences
}

/**
 * TODO: Check if objects have same keys
 * @param {object} obj1 - First object
 * @param {object} obj2 - Second object
 * @returns {boolean} - True if same keys
 */
export function sameKeys(obj1, obj2) {
  // TODO: Check if same keys
}

/**
 * TODO: Compare object lengths
 * @param {object} obj1 - First object
 * @param {object} obj2 - Second object
 * @returns {number} - -1, 0, or 1
 */
export function compareLength(obj1, obj2) {
  // TODO: Compare number of keys
}

/**
 * TODO: Check if objects intersect
 * @param {object} obj1 - First object
 * @param {object} obj2 - Second object
 * @returns {boolean} - True if they share keys
 */
export function objectsIntersect(obj1, obj2) {
  // TODO: Check for shared keys
}

/**
 * TODO: Symmetric difference of objects
 * @param {object} obj1 - First object
 * @param {object} obj2 - Second object
 * @returns {object} - Keys only in one object
 */
export function symmetricDifference(obj1, obj2) {
  // TODO: Get keys in either but not both
}

/**
 * TODO: Subset check
 * @param {object} subset - Potential subset
 * @param {object} superset - Superset
 * @returns {boolean} - True if subset
 */
export function isSubset(subset, superset) {
  // TODO: Check if subset
}

/**
 * TODO: Check object equality with tolerance
 * @param {object} obj1 - First object
 * @param {object} obj2 - Second object
 * @param {number} tolerance - Numeric tolerance
 * @returns {boolean} - True if equal within tolerance
 */
export function fuzzyCompare(obj1, obj2, tolerance) {
  // TODO: Compare with numeric tolerance
}
