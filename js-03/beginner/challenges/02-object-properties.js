/**
 * Challenge 02: Object Properties
 * 
 * Add, update, and delete object properties.
 */

/**
 * TODO: Add a new property to object
 * @param {object} obj - Target object
 * @param {string} key - Property key
 * @param {any} value - Property value
 * @returns {object} - Modified object
 */
export function addProperty(obj, key, value) {
  // TODO: Add new property
}

/**
 * TODO: Update existing property
 * @param {object} obj - Target object
 * @param {string} key - Property key
 * @param {any} value - New value
 * @returns {boolean} - True if updated, false if not found
 */
export function updateProperty(obj, key, value) {
  // TODO: Update property if exists
}

/**
 * TODO: Delete a property
 * @param {object} obj - Target object
 * @param {string} key - Property key
 * @returns {boolean} - True if deleted, false if not found
 */
export function deleteProperty(obj, key) {
  // TODO: Delete property
}

/**
 * TODO: Merge two objects
 * @param {object} obj1 - First object
 * @param {object} obj2 - Second object
 * @returns {object} - Merged object
 */
export function mergeObjects(obj1, obj2) {
  // TODO: Merge objects (obj2 overwrites obj1)
}

/**
 * TODO: Get object without specified keys
 * @param {object} obj - Source object
 * @param {string[]} keys - Keys to exclude
 * @returns {object} - Object without specified keys
 */
export function omitProperties(obj, keys) {
  // TODO: Return object without specified keys
}

/**
 * TODO: Update multiple properties at once
 * @param {object} obj - Target object
 * @param {object} updates - Properties to update
 * @returns {object} - Modified object
 */
export function updateMultiple(obj, updates) {
  // TODO: Update multiple properties
}

/**
 * TODO: Toggle boolean property
 * @param {object} obj - Target object
 * @param {string} key - Property key
 * @returns {boolean} - New value
 */
export function toggleBoolean(obj, key) {
  // TODO: Toggle boolean value
}

/**
 * TODO: Increment numeric property
 * @param {object} obj - Target object
 * @param {string} key - Property key
 * @param {number} amount - Amount to increment
 * @returns {number} - New value
 */
export function incrementProperty(obj, key, amount = 1) {
  // TODO: Increment numeric property
}
