/**
 * Challenge 03: Object Methods
 * 
 * Add and use methods inside objects.
 */

/**
 * TODO: Create object with method to calculate area
 * @param {number} width - Width
 * @param {number} height - Height
 * @returns {object} - Object with width, height, and area method
 */
export function createRectangle(width, height) {
  // TODO: Create rectangle object with area() method
}

/**
 * TODO: Create counter object with increment/decrement
 * @param {number} initial - Initial value
 * @returns {object} - Counter object with methods
 */
export function createCounter(initial = 0) {
  // TODO: Create counter with increment, decrement, get methods
}

/**
 * TODO: Create user object with greeting method
 * @param {string} name - User name
 * @returns {object} - User object with greet method
 */
export function createUser(name) {
  // TODO: Create user with name and greet() method
}

/**
 * TODO: Create calculator object
 * @returns {object} - Calculator with add, subtract, multiply, divide
 */
export function createCalculator() {
  // TODO: Create calculator with arithmetic methods
}

/**
 * TODO: Create book object with pages
 * @param {string} title - Book title
 * @param {number} totalPages - Total pages
 * @returns {object} - Book object with read method
 */
export function createBook(title, totalPages) {
  // TODO: Create book with currentPage and read(n) method
}

/**
 * TODO: Create bank account
 * @param {number} initialBalance - Initial balance
 * @returns {object} - Account with deposit, withdraw, getBalance
 */
export function createAccount(initialBalance = 0) {
  // TODO: Create account with balance and transaction methods
}

/**
 * TODO: Create stack data structure
 * @returns {object} - Stack with push, pop, peek, isEmpty
 */
export function createStack() {
  // TODO: Create stack with LIFO operations
}

/**
 * TODO: Create queue data structure
 * @returns {object} - Queue with enqueue, dequeue, front, isEmpty
 */
export function createQueue() {
  // TODO: Create queue with FIFO operations
}
