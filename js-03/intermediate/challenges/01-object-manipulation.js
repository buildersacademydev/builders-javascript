/**
 * Challenge 01: Object Manipulation
 * 
 * Complex object operations and transformations.
 */

/**
 * TODO: Deep freeze an object
 * @param {object} obj - Object to freeze
 * @returns {object} - Frozen object
 */
export function deepFreeze(obj) {
  // TODO: Recursively freeze object
}

/**
 * TODO: Deep seal an object
 * @param {object} obj - Object to seal
 * @returns {object} - Sealed object
 */
export function deepSeal(obj) {
  // TODO: Recursively seal object
}

/**
 * TODO: Get object size (number of keys)
 * @param {object} obj - Object to measure
 * @returns {number} - Number of keys
 */
export function getObjectSize(obj) {
  // TODO: Return count of own enumerable properties
}

/**
 * TODO: Check if object is empty
 * @param {object} obj - Object to check
 * @returns {boolean} - True if empty
 */
export function isObjectEmpty(obj) {
  // TODO: Check if object has no properties
}

/**
 * TODO: Convert object to entries
 * @param {object} obj - Object to convert
 * @returns {Array} - Array of [key, value] pairs
 */
export function toEntries(obj) {
  // TODO: Convert object to entries
}

/**
 * TODO: Create object from entries
 * @param {Array} entries - Array of [key, value] pairs
 * @returns {object} - Created object
 */
export function fromEntries(entries) {
  // TODO: Create object from entries
}

/**
 * TODO: Map object values
 * @param {object} obj - Source object
 * @param {function} fn - Mapping function
 * @returns {object} - Mapped object
 */
export function mapObjectValues(obj, fn) {
  // TODO: Map over object values
}

/**
 * TODO: Filter object properties
 * @param {object} obj - Source object
 * @param {function} predicate - Filter predicate
 * @returns {object} - Filtered object
 */
export function filterObject(obj, predicate) {
  // TODO: Filter object by predicate
}

/**
 * TODO: Invert object keys and values
 * @param {object} obj - Source object
 * @returns {object} - Inverted object
 */
export function invertObject(obj) {
  // TODO: Swap keys and values
}

/**
 * TODO: Create object with numeric keys
 * @param {any[]} arr - Array of values
 * @returns {object} - Object with numeric keys
 */
export function arrayToObject(arr) {
  // TODO: Convert array to object with indices
}

/**
 * TODO: Pick random properties from object
 * @param {object} obj - Source object
 * @param {number} n - Number of properties
 * @returns {object} - Random properties
 */
export function randomProperties(obj, n) {
  // TODO: Pick n random properties
}

/**
 * TODO: Sort object by values
 * @param {object} obj - Source object
 * @returns {object} - Sorted object
 */
export function sortByValue(obj) {
  // TODO: Sort object by values
}

/**
 * TODO: Check if objects are deeply equal
 * @param {object} obj1 - First object
 * @param {object} obj2 - Second object
 * @returns {boolean} - True if deeply equal
 */
export function deepEqual(obj1, obj2) {
  // TODO: Deep comparison
}

/**
 * TODO: Get difference between objects
 * @param {object} obj1 - First object
 * @param {object} obj2 - Second object
 * @returns {object} - Object with differences
 */
export function objectDifference(obj1, obj2) {
  // TODO: Get properties that differ
}

/**
 * TODO: Create object with only own properties
 * @param {object} obj - Source object
 * @returns {object} - Object with only own properties
 */
export function getOwnProperties(obj) {
  // TODO: Get only own properties
}
