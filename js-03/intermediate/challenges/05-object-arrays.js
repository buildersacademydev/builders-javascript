/**
 * Challenge 05: Object Arrays
 * 
 * Work with arrays of objects.
 */

/**
 * TODO: Find object by property
 * @param {object[]} arr - Array of objects
 * @param {string} prop - Property name
 * @param {any} value - Property value
 * @returns {object|undefined} - Found object
 */
export function findByProperty(arr, prop, value) {
  // TODO: Find object with matching property
}

/**
 * TODO: Sort array of objects by property
 * @param {object[]} arr - Array of objects
 * @param {string} prop - Property to sort by
 * @returns {object[]} - Sorted array
 */
export function sortByProperty(arr, prop) {
  // TODO: Sort by property
}

/**
 * TODO: Group array of objects by property
 * @param {object[]} arr - Array of objects
 * @param {string} prop - Property to group by
 * @returns {object} - Grouped object
 */
export function groupByProperty(arr, prop) {
  // TODO: Group objects by property
}

/**
 * TODO: Unique by property
 * @param {object[]} arr - Array of objects
 * @param {string} prop - Property to get unique by
 * @returns {object[]} - Array with unique property values
 */
export function uniqueByProperty(arr, prop) {
  // TODO: Get unique by property
}

/**
 * TODO: Sum property values
 * @param {object[]} arr - Array of objects
 * @param {string} prop - Property to sum
 * @returns {number} - Sum of property values
 */
export function sumByProperty(arr, prop) {
  // TODO: Sum property values
}

/**
 * TODO: Average property values
 * @param {object[]} arr - Array of objects
 * @param {string} prop - Property to average
 * @returns {number} - Average of property values
 */
export function averageByProperty(arr, prop) {
  // TODO: Calculate average
}

/**
 * TODO: Filter and map objects
 * @param {object[]} arr - Array of objects
 * @param {function} filterFn - Filter function
 * @param {function} mapFn - Map function
 * @returns {any[]} - Filtered and mapped result
 */
export function filterAndMapObjects(arr, filterFn, mapFn) {
  // TODO: Filter then map
}

/**
 * TODO: Check if all objects have property value
 * @param {object[]} arr - Array of objects
 * @param {string} prop - Property name
 * @param {any} value - Value to check
 * @returns {boolean} - True if all have value
 */
export function allHaveProperty(arr, prop, value) {
  // TODO: Check if all have property value
}

/**
 * TODO: Partition objects by property
 * @param {object[]} arr - Array of objects
 * @param {string} prop - Property to partition by
 * @returns {object} - { true: [], false: [] }
 */
export function partitionByProperty(arr, prop) {
  // TODO: Partition by truthy property value
}

/**
 * TODO: Index array by property
 * @param {object[]} arr - Array of objects
 * @param {string} prop - Property to index by
 * @returns {object} - Object indexed by property
 */
export function indexByProperty(arr, prop) {
  // TODO: Create index by property
}
